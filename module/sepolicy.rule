# Define the new context for the Flipendo feature
type flipendo, domain;

# Define the file type associated with flipendo
type flipendo_file, file_type;

# Define the userfaultfd type for flipendo
type flipendo_userfaultfd;

# Associate flipendo with the generic app domain
# This allows it to behave like a standard process
typeattribute flipendo appdomain;
